# SimpCalc

Run the `main.py` program in the terminal by passing the input filepath or input directory as the first paramter e.g. `python main.py samples` or `python main.py samples/sample_input_1.txt`
